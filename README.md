# DFS-Traversal---Graphs
This C++ program implements an undirected graph using an adjacency list and performs Depth-First Search (DFS). Starting from vertex 0, DFS explores nodes recursively, visiting neighbors before backtracking, with time complexity O(V + E). It prints the adjacency list and then outputs the DFS traversal order.
<br>
It is an important part in graphs data structure.